# modules/terminal-tools/default.nix
# Terminal tools configuration (will be used by home-manager)
#
# Note: This module is a placeholder for terminal tools configuration.
# Terminal-specific tools (fish, zellij, etc.) are configured directly
# in home-manager context. See home/hbohlen/home.nix for actual implementations.

{ ... }:

{
  # This module provides the structure for terminal tools
  # Actual configuration will be applied in home-manager context
}
