# modules/home-manager/nixvim/lsp/settings.nix
# LSP global settings for nixvim

{ ... }:

{
  programs.nixvim = {
    # LSP global settings can be added here
    # Currently handled by individual plugin configurations
    # Additional LSP settings can be added as needed
  };
}